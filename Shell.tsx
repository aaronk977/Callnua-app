import Link from 'next/link';
const links=[['/dashboard','Dashboard'],['/enquiries','Enquiries'],['/appointments','Appointments & Jobs'],['/services','Services & Pricing'],['/knowledge','AI Receptionist'],['/integrations','Connections'],['/settings','Settings'],['/admin','Master Admin']];
export default function Shell({children,title}:{children:React.ReactNode,title:string}){return <div className="shell"><aside className="side"><div className="brand">callnua<span>•</span></div><nav className="nav">{links.map(([h,l])=><Link key={h} href={h}>{l}</Link>)}</nav></aside><main className="main"><div className="top"><div><h1>{title}</h1><div className="muted">Every enquiry captured. Every next action clear.</div></div><span className="pill">Callnua Production</span></div>{children}</main></div>}
