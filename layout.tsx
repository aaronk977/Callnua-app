import './globals.css';
export const metadata={title:'Callnua',description:'Every enquiry captured. Every next action clear. Nothing forgotten.'};
export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}
